# nwo-orch-reactive-trigger-engine.ts
Transforms projection‑forwarded events into deterministic workflow triggers. Validates trigger envelopes, resolves contract‑defined reactive bindings, and launches workflows through the orchestration engine. Guarantees reproducible, immutable, audit‑aligned reactive behavior across Beast System 3.0.
